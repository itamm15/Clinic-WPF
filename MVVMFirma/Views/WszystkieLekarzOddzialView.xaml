<local:WszystkieViewBase x:Class="MVVMFirma.Views.WszystkieLekarzOddzialView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="white">
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="DoktorOddzialId" Binding="{Binding DoktorOddzialId}"></DataGridTextColumn>
                <DataGridTextColumn Header="Data dolaczenia" Binding="{Binding DataDolaczenia}"></DataGridTextColumn>
                <DataGridTextColumn Header="Imie i nazwisko lekarza" Binding="{Binding LekarzImieNazwisko}"></DataGridTextColumn>
                <DataGridTextColumn Header="Specjalizacja" Binding="{Binding LekarzSpecjalizacja}"></DataGridTextColumn>
                <DataGridTextColumn Header="Nazwa oddzialu" Binding="{Binding OddzialNazwa}"></DataGridTextColumn>
                <DataGridTextColumn Header="Lokalizacja oddzialu" Binding="{Binding OddzialLokalizacja}"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
