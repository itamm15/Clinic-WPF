<local:WszystkieViewBase x:Class="MVVMFirma.Views.WszystkiePersonelOddzialView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="white">
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="PersonelOddzialId" Binding="{Binding PersonelOddzialId}"/>
                <DataGridTextColumn Header="Data dolaczenia" Binding="{Binding DataDolaczenia}"/>
                <DataGridTextColumn Header="Nazwa oddzialu" Binding="{Binding OddzialNazwaOddzialu}"/>
                <DataGridTextColumn Header="Lokalizacja oddzialu" Binding="{Binding OddzialLokalizacja}"/>
                <DataGridTextColumn Header="Data otwarcia oddzialu" Binding="{Binding OddzialDataOtwarcia}"/>
                <DataGridTextColumn Header="Godnosc personelu" Binding="{Binding PersonelImieNazwisko}"/>
                <DataGridTextColumn Header="Stanowisko personelu" Binding="{Binding PersonelStanowisko}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
